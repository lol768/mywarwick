# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                       @controllers.ApplicationController.index

->         /admin                  admin.Routes

GET        /test/inject            @controllers.ApplicationController.showInjectForm
POST       /test/inject            @controllers.ApplicationController.injectEvent

GET        /websockets/page        @controllers.ApplicationController.socket

GET        /start.appcache         @controllers.AppCacheController.cacheManifest

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file           @controllers.Assets.versioned(path="/public", file: Asset)