# --- !Ups
CREATE TABLE PUSH_SUBSCRIPTION (
  "REGISTRATION_ID" NVARCHAR2(4000),
  "USERCODE" NVARCHAR2(100) NOT NULL,
  "PLATFORM" NVARCHAR2(200) NOT NULL,
  CONSTRAINT "PUSH_SUBSCRIPTION_PK" PRIMARY KEY ("REGISTRATION_ID")
);

# --- !Downs
DROP TABLE PUSH_SUBSCRIPTIONS;

