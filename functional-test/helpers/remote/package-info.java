/**
 *  Support for tests that run against a deployed instance of the app
 *  (this can still be a local instance running on your computer, but
 *  it's otherwise a standalone server)
 */
package helpers.remote;