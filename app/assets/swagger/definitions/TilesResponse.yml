type: object
properties:
  success:
    type: boolean
  status:
    type: string
  data:
    properties:
      tiles:
        readOnly: true
        type: array
        items:
          $ref: '#/definitions/Tile'

      layout:
        readOnly: true
        type: array
        items:
          $ref: '#/definitions/TileLayout'